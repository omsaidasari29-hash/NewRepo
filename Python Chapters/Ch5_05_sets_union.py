# To take both the values in two sets

s1 = {1, 2, 3, 45}

s2 = {4 ,5 ,1 ,1154, 1145, 78, 1, 2, 3}

# print(s1.union(s2)) # To take s1 and s2 both in single sets
# print(s1.intersection(s2)) # To get the repeated values only 

print({1, 2, 3}.issubset(s2)) # to set value is preset in that set or not